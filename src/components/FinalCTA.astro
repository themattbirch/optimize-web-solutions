---
// src/components/FinalCTA.astro
---

<section
  id="final-cta"
  class="relative overflow-hidden bg-white dark:bg-background text-center text-background dark:text-white"
>
  <!-- Gradient backdrop -->
  <div
    class="absolute inset-0 z-0 bg-gradient-to-tr from-[#FF7E00] via-[#FF9500] to-[#FF5200]"
  >
  </div>

  <!-- Top wave -->
  <div class="absolute top-0 left-0 w-full overflow-hidden leading-[0] z-10">
    <svg
      class="relative block w-[calc(140%+1px)] h-[60px] sm:h-[100px] -translate-x-[20%] fill-white dark:fill-[#121212]"
      viewBox="0 0 1440 100"
      preserveAspectRatio="none"
    >
      <path
        d="M0,100 C240,90 480,20 720,20 C960,20 1200,90 1440,100 L1440,0 L0,0 Z"
      ></path>
    </svg>
  </div>

  <!-- Bottom wave -->
  <div
    class="absolute bottom-0 left-0 w-full overflow-hidden leading-[0] z-10 rotate-180"
  >
    <svg
      class="relative block fill-white dark:fill-[#121212] w-[calc(140%+1px)] h-[60px] sm:h-[100px] -translate-x-[20%]"
      viewBox="0 0 1440 100"
      preserveAspectRatio="none"
    >
      <path
        d="M0,100 C240,90 480,20 720,20 C960,20 1200,90 1440,100 L1440,0 L0,0 Z"
      ></path>
    </svg>
  </div>

  <!-- Content -->
  <div
    class="relative z-20 mx-auto max-w-2xl px-4 py-[72px] sm:px-6 sm:py-[72px] md:px-8 lg:px-0"
  >
    <style is:global>
      #final-cta h2,
      #final-cta p {
        color: #111; /* default */
      }
      #final-cta a {
        background: #111;
        color: #fff;
        border: none;
        text-shadow: none;
      }
      #final-cta a:hover {
        background: #333;
      }

      .dark #final-cta h2,
      .dark #final-cta p {
        color: #111; /* stronger contrast against orange */
        text-shadow: none; /* remove glow, improves sharpness */
      }

      .dark #final-cta .bg-gradient-to-tr {
        background-image: linear-gradient(
          to top right,
          #cc5200,
          #e65c00,
          #ff7e00
        );
      }
    </style>

    <h2
      class="font-heading text-3xl font-extrabold tracking-tight sm:text-4xl lg:text-5xl"
    >
      Ready to Build a Better Website?
    </h2>

    <p class="mx-auto mt-6 max-w-lg text-base sm:text-lg">
      Whether you’re launching a new site or improving an existing one, we help
      small businesses create fast, clear, and reliable websites that are easy
      to manage and built to grow.
    </p>

    <a
      href="/contact"
      class="mt-8 inline-flex items-center justify-center rounded-xl bg-white/95 px-8 py-3 text-base font-semibold text-[#ff5200] shadow-lg ring-2 ring-white transition-all duration-300 hover:-translate-y-1 hover:bg-transparent hover:text-white hover:shadow-xl"
    >
      Get a Quote
    </a>
  </div>
</section>
